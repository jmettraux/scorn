
name: Test

on: [ push, pull_request ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: ruby --version
      - run: gem --version
      - run: bundle --version
      - run: bundle exec rspec

